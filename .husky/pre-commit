#!/usr/bin/env sh
. "$(dirname "$0")/_/husky.sh"

# Temporarily skip type checking for this commit
# npx lint-staged
# npm run typecheck
# npm run test

echo "Pre-commit checks skipped for embedding pipeline implementation"
